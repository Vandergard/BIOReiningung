  def search
    @clients = Client.find_by(search_params1)(3)||Client.find_by(search_params2)(3)||Client.find_by(search_params3)(3)
        #redirect_to :controller => :clients, :action => :search_clients
    render 'clients/search_clients'
  end

  def search_clients
    @clients = Client.find_by(search_params1)(3)||Client.find_by(search_params2)(3)||Client.find_by(search_params3)(3)
  end

  def client_params
      params.require(:client).permit(:name, :phone, :vorname, :patronymic,
                                     :sum, :date_in)
  end


    def search
    if search_params1 ==nil

    @clients = Client.where(search_params1)||Client.where(search_params2)||Client.where(search_params3)
    render 'clients/search_clients'
  end

  def search_clients
    @clients = Client.where(search_params1)||Client.where(search_params2)||Client.where(search_params3)
  end