<% if @technpassport.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@technpassport.errors.count, "error") %>.
    </div>
    <ul>
    <% @technpassport.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>